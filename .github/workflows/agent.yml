name: Agent NI3 Java Copilot Bot

on:
  issue_comment:
    types: [created]

permissions:
  contents: write
  issues: write

jobs:
  agent-ni3:
    if: startsWith(github.event.comment.body, '[agent-ni3]')
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout main branch
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      # 2️⃣ Setup Node.js (needed for Copilot CLI)
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "20"

      # 3️⃣ Install Copilot CLI locally
      - name: Install Copilot CLI
        run: npm install @githubnext/github-copilot-cli


      # 5️⃣ Authenticate Copilot CLI
      - name: Authenticate Copilot CLI
        run: npx copilot auth login --with-token ${{ secrets.GITHUB_TOKEN }}

      # 6️⃣ Parse issue comment for prompt
      - name: Parse Comment
        id: parse
        run: |
          COMMENT="${{ github.event.comment.body }}"
          ISSUE_NUMBER="${{ github.event.issue.number }}"

          if [[ "$COMMENT" =~ \[agent-ni3\](.*) ]]; then
              PROMPT="${BASH_REMATCH[1]}"
              PROMPT=$(echo "$PROMPT" | sed 's/^[[:space:]]*//;s/[[:space:]]*$//')
          else
              PROMPT=""
          fi

          if [ -z "$PROMPT" ]; then
              curl -s -X POST \
                -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
                -H "Accept: application/vnd.github+json" \
                https://api.github.com/repos/${{ github.repository }}/issues/${ISSUE_NUMBER}/comments \
                -d '{"body":"❌ No valid prompt detected. Please start your comment with `[agent-ni3]` followed by instructions."}'
              exit 1
          fi

          echo "PROMPT=$PROMPT" >> $GITHUB_ENV

      # 7️⃣ Post in-progress comment
      - name: Post in-progress comment
        run: |
          curl -s -X POST \
            -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
            -H "Accept: application/vnd.github+json" \
            https://api.github.com/repos/${{ github.repository }}/issues/${{ github.event.issue.number }}/comments \
            -d '{"body":"⏳ Agent NI3 is processing your Java prompt on the main branch..."}'

      # 8️⃣ Generate Java code using Copilot CLI via npx
      - name: Generate Java code with Copilot
        run: |
          PROMPT="${{ env.PROMPT }}"
          npx copilot generate --language java --prompt "$PROMPT" > agent_output.txt
          cat agent_output.txt

      # 9️⃣ Parse multi-file AI output and create files
      - name: Parse AI output to files
        run: |
          awk '
            /^file:/ { file=$2; next }
            /^content: \|/ { content=""; in_content=1; next }
            { if(in_content) { content=content $0 "\n" } }
            /^$/ { if(file != "") { 
                      system("mkdir -p \"$(dirname \""file"\")\" && echo \"" content "\" > \""file"\"") 
                      file=""; in_content=0 
                    } 
            }
          ' agent_output.txt || echo "⚠️ Some AI output could not be parsed; check agent_output.txt"

      # 🔟 Commit and push changes to main
      - name: Commit changes
        run: |
          git config user.name "Agent NI3 Bot"
          git config user.email "agent-ni3-bot@github.com"
          git add .
          git diff-index --quiet HEAD || git commit -m "Agent NI3 Copilot-generated Java files"
          git push origin main || echo "⚠️ Push failed; possibly no changes"

      # 1️⃣1️⃣ Post preview in the issue
      - name: Post result
        run: |
          AI_PREVIEW=$(head -n 50 agent_output.txt)
          curl -s -X POST \
            -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
            -H "Accept: application/vnd.github+json" \
            https://api.github.com/repos/${{ github.repository }}/issues/${{ github.event.issue.number }}/comments \
            -d '{"body": "✅ Agent NI3 applied Copilot-generated Java files on the main branch.\n\nPreview (first 50 lines):\n'"$AI_PREVIEW"'"}'
